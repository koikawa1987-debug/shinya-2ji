// 局の定数。編成ロジックはすべてここの数値で決定論的に動く。

// 放送日の境界。日本のラテ欄の慣習にあわせ 05:00 起点、28:59 終端とする。
// つまり深夜 26:00 は「前日の紙面」に載る。局名が「深夜二時」である以上、
// 深夜帯が紙面の途中で翌日に切り替わってしまうのは具合が悪い。
export const DAY_START_MIN = 5 * 60; // 05:00
export const DAY_END_MIN = 29 * 60; // 翌 05:00

export const WEEKDAYS = ['日', '月', '火', '水', '木', '金', '土'];

export const MODEL = 'claude-sonnet-4-6';

// 視聴率シミュレーション ------------------------------------------------

// 時間帯係数（放送日内の開始時刻の「時」で引く。5〜28）
export const TIME_COEFFICIENT = {
  5: 0.3, 6: 0.5, 7: 0.6, 8: 0.7, 9: 0.7, 10: 0.7, 11: 0.8,
  12: 1.1, 13: 0.8, 14: 0.7, 15: 0.7, 16: 0.7, 17: 0.9,
  18: 1.3, 19: 1.5, 20: 1.6, 21: 1.5, 22: 1.3, 23: 1.1,
  24: 0.9, 25: 0.7, 26: 0.6, 27: 0.4, 28: 0.3,
};

// ジャンル係数
export const GENRE_COEFFICIENT = {
  報道: 2.6,
  通販: 1.1,
  情報: 2.0,
  ドキュメンタリー: 1.7,
  音楽: 1.6,
  バラエティ: 2.2,
  ドラマ: 2.1,
  演芸: 1.5,
  映画: 2.3,
  トーク: 1.4,
  実験音楽: 0.9,
  教養: 1.5,
  再放送: 1.2,
};
export const GENRE_COEFFICIENT_DEFAULT = 1.5;

// 継続週数による減衰。放送開始からの週数が増えるほど数字は落ちる。
export const DECAY_PER_WEEK = 0.985;
export const DECAY_FLOOR = 0.62;

export const RATING_JITTER = 0.8; // ±0.8
export const RATING_MIN = 0.1;

// CM ------------------------------------------------------------------

// 番組と番組の間（ステーションブレイク）に積めるCMの秒数
export const SB_MAX_SECONDS = 30;
// 番組内（パーティシペーション）に積めるCMの秒数
export const PT_MAX_SECONDS = 45;
// 何分以上の番組に PT ブレイクを入れるか
export const PT_MIN_PROGRAM_MINUTES = 30;

export const 休止タイトル = '放送休止（カラーバー）';
